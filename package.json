{
  "name": "vscode-faker",
  "displayName": "vscode-faker",
  "description": "y",
  "version": "0.0.1",
  "publisher": "deerawan",
  "engines": {
    "vscode": "^0.10.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.fakerAddress",
    "onCommand:extension.fakerCommerce",
    "onCommand:extension.fakerCompany",
    "onCommand:extension.fakerDate",
    "onCommand:extension.fakerFinance"
  ],
  "main": "./out/src/extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.fakerAddress",
        "title": "Faker: Address"
      },
      {
        "command": "extension.fakerCommerce",
        "title": "Faker: Commerce"
      },
      {
        "command": "extension.fakerCompany",
        "title": "Faker: Company"
      },
      {
        "command": "extension.fakerDate",
        "title": "Faker: Date"
      },
      {
        "command": "extension.fakerFinance",
        "title": "Faker: Finance"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "node ./node_modules/vscode/bin/compile",
    "compile": "node ./node_modules/vscode/bin/compile -watch -p ./"
  },
  "devDependencies": {
    "typescript": "^1.6.2",
    "vscode": "0.10.x"
  },
  "dependencies": {
    "faker": "^3.1.0"
  }
}
